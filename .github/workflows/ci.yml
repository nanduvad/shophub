name: CI/CD Pipeline

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: write

jobs:
  backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17 and Maven
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: maven

      - name: Build backend
        working-directory: backend(cicd)
        run: mvn clean package -DskipTests

      - name: Run backend tests
        working-directory: backend(cicd)
        run: mvn test

  frontend:
    runs-on: ubuntu-latest
    needs: backend   # frontend runs only after backend succeeds

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install frontend dependencies
        working-directory: frontend(cicd)
        run: npm ci

      - name: Run frontend tests
        working-directory: frontend(cicd)
        run: npm test --if-present

      - name: Build frontend
        working-directory: frontend(cicd)
        run: npm run build

      - name: Deploy frontend to GitHub Pages
        working-directory: frontend(cicd)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "nanduvad"
          git config --global user.email "rajanandinivadlamudi8008@gmail.com"
          npm install -g gh-pages
          gh-pages -d build -r https://x-access-token:${GITHUB_TOKEN}@github.com/nanduvad/shophub.git
